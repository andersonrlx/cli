apiVersion: v1
kind: Service
metadata:
  name: <%= name %>
  namespace: <%= namespace %>
spec:
  type: ClusterIP
  selector:
    app: <%= name %>
  ports:
    - port: 80
      targetPort: <%= port %>
      protocol: TCP
